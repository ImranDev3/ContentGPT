# Frontend reads no secrets — the Manifest API key lives on the backend.
# Set NEXT_PUBLIC_API_URL only if your backend is on a different origin
# in production (defaults to same-origin rewrites).
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
